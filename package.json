{
  "name": "@erauner/obsidian-tools",
  "version": "0.7.0",
  "description": "Quick capture and inbox for Obsidian vaults using mdbase",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "obsidian-tools": "./dist/cli.js"
  },
  "publishConfig": {
    "registry": "https://nexus.erauner.dev/repository/npm-hosted/"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@erauner/mdbase": "^0.1.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
